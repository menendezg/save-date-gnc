<%= form_with model: photo, data: { turbo_frame: 'photo_form' }, local: true, html: { multipart: true } do |form| %>
  <div>
    form label
    <%= form.label :image %>
    <%= form.file_field :image, accept: 'image/*' %>
  </div>
  <div>
    <%= form.submit "Upload Photo" %>
  </div>
<% end %>


  <% if photo.image.attached? %>
    <div>
      <%= image_tag(photo.image, class: "h-auto max-w-full rounded-lg") %>
      <div class="p-3">
        <p class="text-sm text-gray-600">#Photo #Inspiration</p>
      </div>
    </div>
  <% end %>
